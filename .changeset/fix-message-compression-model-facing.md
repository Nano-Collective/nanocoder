---
'@nanocollective/nanocoder': patch
---

Ensure mechanical message compression and token counting filter out display-only messages using `isModelFacing` and `filterModelFacing`, keeping visibility handling consistent across all compaction strategies.
