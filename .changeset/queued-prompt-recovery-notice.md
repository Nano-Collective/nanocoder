---
'@nanocollective/nanocoder': patch
---

Show a recovery hint when a queued prompt fails to dispatch so it can be selected and edited instead of leaving the queue blocked without feedback. Refs #1060.
