name: k8s
description: Kubernetes operational helpers - list pods, fetch logs, validate manifests.
version: 0.1.0
tools_visibility:
  default: scoped

subscribe:
  - kind: file.changed
    target: agent:k8s_agent
    paths:
      - "k8s/**/*.yaml"
    confirm: true